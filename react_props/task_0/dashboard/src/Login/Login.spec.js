import { render } from "@testing-library/react";
import Login from "./Login";

test("rendu du composant Login", () => {
  render(<Login />);
  // Ajouter des tests pour le formulaire de login ici
});
